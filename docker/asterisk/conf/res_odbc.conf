[asterisk]
enabled => yes
dsn => asterisk-connector
username => sail
password => password
; Performance: persistent connection pool
pre-connect => yes
max_connections => 50
; Connection health check timeout
connect_timeout => 10
; Log slow queries
logging => no
